# Copy to .env.local and adjust. Never commit .env.local.
#
# Anything prefixed NEXT_PUBLIC_ is shipped to the browser, so no secret may go here.
# Provider keys live in server/.env and are reached through the server.

NEXT_PUBLIC_API_URL=http://localhost:4000
