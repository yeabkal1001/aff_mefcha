packages:
  - client

# The server is a Python package and is not part of the pnpm workspace; see
# server/README.md. Root scripts still drive both halves.

# pnpm 10 blocks dependency install scripts by default. This is the allowlist.
# Anything not listed here stays blocked, which is the point.
onlyBuiltDependencies:
  - esbuild
