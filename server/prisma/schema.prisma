// The data model is specified in ../../docs/architecture/session-engine.md, section 11.
// It is deliberately not written here yet — scaffolding only.
//
// When you do write it, the names are binding: `learner_*` not `user_*`, `turn`
// separate from `attempt`, and no table for a Profile Dimension or a learning plan.
// See ../../CONTEXT.md and the `coach-canon` skill.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
